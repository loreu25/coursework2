@model IEnumerable<MusicCatalog.Models.Artist>

@{
    ViewData["Title"] = "Исполнители";
}

<h1>Исполнители</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Добавить нового исполнителя</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bio)
            </th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtistId" class="btn btn-sm btn-warning">Изменить</a>
                <a asp-action="Details" asp-route-id="@item.ArtistId" class="btn btn-sm btn-info">Подробно</a>
                <a asp-action="Delete" asp-route-id="@item.ArtistId" class="btn btn-sm btn-danger">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
